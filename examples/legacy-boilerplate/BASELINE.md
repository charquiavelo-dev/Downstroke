# Preserved Baseline

This directory preserves the pre-framework documentation, scripts, skills and archive used as extraction evidence. Downstroke development happens in the repository root; this example changes only when a deliberate baseline snapshot is created.
