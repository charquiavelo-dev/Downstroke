---
scope: Downstroke framework
purpose: build substrate
altitude: feature
updated: 2026-07-01T19:07
---

- (constraint) Existing npm workspaces and strict TypeScript Node ESM package boundaries are adopted.
- (decision) Use a functional core and imperative shell: capability packages declare intent; core plans and executes effects; CLI owns interaction.
- (decision) All mutations follow inspect, plan, authorize, apply, verify; dry-run stops before apply.
- (decision) Repository state is local to .downstroke and workspace state to .downstroke-workspace; repository roots never share mutable state.
- (decision) Authorization is capability-scoped by operation, target and lifetime; local commit and push are separate capabilities.
- (decision) External Breakdown Stack tools remain detected and invoked through canonical commands until evidence gates a native successor.
- (decision) PostgreSQL or the selected application backend owns operational data and permissions; frontend and LLM output are never authorities.
- (decision) Design system and localization catalogs are sources of truth; LLM-specific instructions and platform assets are generated projections.
- (decision) Release artifacts come from explicit manifests and clean verification; public history replacement requires verified private history and fresh approval.
- (direction) Defer provider-specific abstractions, native replacements, agent runtime and registry internals until their implementing epic begins.
- (decision) Plan composition rejects duplicate targets unless byte-equivalent or distinct valid Managed Block ownership prevents conflict.
- (decision) Project and Workspace state use schema validation, revision checks and atomic replacement.
- (event) Architecture spine finalized.
